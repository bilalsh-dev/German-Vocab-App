# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase
- Feature 10 (AI card drafting) complete. Roadmap (01–10) complete.

## Current Goal
- None — all roadmap features implemented.

## Completed
- Project scaffolded: Next.js 16 (App Router) + React 19 + TypeScript + Tailwind 4, pnpm. (commit `01a595e`)
- Vitest + React Testing Library configured; `test` / `test:watch` / `test:coverage` scripts.
- Boilerplate cleaned: `globals.css` stripped to `@import "tailwindcss";`, all `public/*.svg` removed, minimal `app/page.tsx` rendering the app name.
- Project named **Wortbox**. (commit `38fe462`)
- Context docs rewritten to capture the full problem: German A1, English-front cards, the Card Content Model (gender colors, vowel-length marks, verb forms, related words, opposites/synonyms, topics/tags, examples), and bilingual (EN/DE) UI.
- **Feature 01 — Design system & UI primitives:** full token set in `globals.css` (theme + state + gender) with `@theme inline` Tailwind mappings, light-mode values under `prefers-color-scheme: light`, and vowel-length mark styling (`.vowel-long` underline / `.vowel-short` positioned-dot). `lib/utils.ts` `cn()` helper. shadcn/ui (new-york) configured via `components.json`; all 10 primitives installed (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea, Badge, Select, Tooltip). `lucide-react` installed. Geist Sans/Mono loaded in `app/layout.tsx`. `test` / `lint` / `build` all pass.
- **Feature 02 — App shell:** `components/shell/top-nav.tsx` (client; sticky, `bg-surface`, bottom border, Wortbox logo, Decks/Study/Stats nav reflecting the active route via `usePathname`, responsive icon-only collapse on narrow viewports, disabled language-switcher placeholder slot for Feature 03) and `components/shell/footer.tsx` (slim, muted, app meta). Wired into `app/layout.tsx`: `body` is `min-h-full flex flex-col`, content `<main>` is centered (`max-w-5xl`) and `flex-1`, footer pinned to bottom. Placeholder route pages added: `/` (decks), `/study` (landing), `/study/[deckId]` (review session), `/stats`. All copy is plain placeholder text, swappable to i18n keys in Feature 03. `test` / `lint` / `build` all pass.
- **Feature 04 — Card data model & storage:** `lib/content` holds the full type contract (`Card`, `Deck`, `ReviewLogEntry`, `Settings`, gender/POS/vowel types, and `SchedulingState` as a discriminated union — FSRS variant for v1, SM-2/Leitner variants stubbed behind the same union). Pure derivation helpers: `genderColorToken` (→ `{gender, article, cssVariable, textClass}` from the noun gender, or null), `genderEndingHint` (suffix rules, longest-match wins), `genderGroupHint` (curated days/months/seasons/weather sets), and validation (`getCardValidationErrors`/`isValidCard`/`assertValidCard` — POS-specific data present iff it matches `partOfSpeech`). `initialSchedulingState(algorithm, now)` builds a sane default scheduling record (real FSRS init arrives with the scheduler in Feature 07). `lib/db` is a Dexie v1 database (`wortbox`) with stores `decks`/`cards`/`reviewLog`/`settings`; cards indexed by `deckId`, `topic`, `scheduling.due`. Per-store data-access helpers (create/read/update/delete; `deleteDeck` cascades cards + reviewLog in a transaction; `createCard`/`updateCard` validate POS data). `dexie@4` added via pnpm. **Locale migration (owner-confirmed: settings canonical + cookie mirror):** `settings` store now holds `uiLanguage`; the locale switcher writes both Dexie (canonical) and the `wortbox.locale` cookie (SSR transport); `components/shell/locale-sync.tsx` reconciles on load — seeds settings from the current locale if absent, else makes the cookie match the canonical setting. The cookie stays as the server-readable mechanism so first paint is correct. Helper tests cover gender color/ending/group hints and validation (17 tests total). `test` / `lint` / `build` all pass.
- **Feature 05 — card rendering:** `components/card` holds the direction-aware renderer. `german-word.tsx` wraps `vowelMarks` over the German string by character index (long → `.vowel-long` underline, short → `.vowel-short` under-dot from Feature 01); unmarked chars render as fragments so an unmarked word stays a single text node. `german-detail.tsx` is the **shared German block** (used by both sides): gender-colored headword via `genderColorToken`, then POS blocks — noun (colored article + plural + ending/group hint, hint prefers stored `NounData` value else derived via `genderEndingHint`/`genderGroupHint`), verb (Präsens/Präteritum/Perfekt form table with auxiliary on Perfekt), adjective (opposites/synonyms as `Badge` chips) — then related-word chips and 3–4 examples (German + English subtext, verb examples labeled by form). `card-prompt.tsx` renders the prompt headword (German side = colored `GermanWord`, minimal chrome; English side = plain text). `card-reveal.tsx` always renders `GermanDetail`, plus the English headword above it only when `promptSide === "german"` (so the German block is identical whichever side German is on). New `PromptSide = "english" | "german"` type in `lib/content`. New `card` i18n namespace (EN/DE): `plural`, `praesens`/`praeteritum`/`perfekt` (German tense names in both locales), `opposites`, `synonyms`, `related`, `examples`. Component tests cover one card per POS, both `promptSide` values, gender colors (masc/fem/neuter), vowel marks, the ending hint, and the identical-German-block invariant (30 tests total). `test` / `lint` / `build` all pass.
- **Feature 06 — deck & card management:** Pure layer in `lib/content`: `card-filter.ts` (`filterCards`/`collectTopics`/`collectTags`), `card-draft.ts` (the editable `CardDraft` form model + `emptyCardDraft`/`cardToDraft`/`buildCardFields`/`draftToPreviewCard` and `getCardDraftErrors` — draft validation that goes beyond `validation.ts`: english/german/topic required, noun needs gender + plural, verb needs all three forms), and scheduling predicates `isNewScheduling`/`isDueScheduling`. `lib/db/summaries.ts` adds `getDeckSummaries()` → decks joined with `{ total, due, new }` counts (single cards scan, predicates from `lib/content`). UI: `components/decks` (`deck-grid` home container, `deck-card`, `deck-form-dialog` create/rename, `deck-detail` deck page container) + `components/cards` (`card-list` with topic/tag filters + add/edit/delete, `card-row`, `card-editor-dialog` structured editor with **live preview** via Feature 05 `CardReveal`, `vowel-mark-field` click-to-cycle long→short→unmarked) + reusable `components/common/confirm-dialog`. New route `app/decks/[deckId]` (deck management); home `app/page.tsx` now renders `DeckGrid`. All writes go through `lib/db` helpers; save is gated on `getCardDraftErrors`; gender ending/group hints surface live in the noun editor. New i18n keys: expanded `decks`/`common` + new `cards` namespace (fields, pos/genders/relations/verbForms options, errors) in EN/DE. Pure tests for `card-draft`, `card-filter`, scheduling predicates (48 tests total). `test` / `lint` / `build` all pass.
- **Feature 07 — review session & scheduler:** Pure `lib/scheduler` wrapping `ts-fsrs@5.4.1` behind one interface: `conversions.ts` maps `FsrsSchedulingState` ↔ ts-fsrs `Card`, `Grade` ↔ `Rating`, `FsrsState` ↔ `State`, and `SchedulerParams` → `FSRSParameters` (via `generatorParameters`); `scheduler.ts` exposes `previewGrades()` (one `repeat()` → all four next states) and `scheduleNext()` (derives the chosen grade **from** `previewGrades`, so previews and applied result are byte-identical); `interval.ts` has the pure `describeInterval(due, now)` → `{unit, value}`. Non-fsrs states throw (`assertFsrs`). Added `learningSteps: number` to `FsrsSchedulingState` so the ts-fsrs round-trip is lossless (tracks the current (re)learning step). UI under `components/study`: `review-session.tsx` (loads deck + due cards + settings, builds the due queue, flip-to-reveal, persist-before-advance, again re-circulates, end-of-session summary, space/1–4 shortcuts), `grade-buttons.tsx` (four grades, each previewing its interval, state-colored), `study-direction-control.tsx` (Select: english-front/german-front/mix — persisted via `setStudyDirection`, re-resolves prompt side, never touches scheduling), `session-summary.tsx` (reviews + lapses). Route `app/study/[deckId]` renders `ReviewSession`; deck-detail header gained a **Study** button (entry point). New `study` i18n keys (EN/DE): remaining/reveal/flipHint/direction/directions/noneDue/grades/interval (ICU plurals)/summary; `decks.study`. Scheduler unit tests cover all four grades, new vs review state, lapse/relearning, preview==apply, retention sensitivity, unsupported-algorithm throw, and interval formatting (13 tests; 61 total). `test` / `lint` / `build` all pass.
- **Feature 03 — i18n (EN/DE):** `next-intl@4` configured **without i18n routing** (no `[locale]` URL segment — fits the local-first single-window app). `next.config.ts` wraps with `createNextIntlPlugin("./src/i18n/request.ts")`. `src/i18n/config.ts` (`locales`/`defaultLocale`/`isLocale`), `src/i18n/locale.ts` (`"use server"` cookie get/set), `src/i18n/request.ts` (`getRequestConfig` reading the cookie + importing messages), `src/global.d.ts` (typed `AppConfig` for key-checked translations). Messages in `messages/en.json` + `messages/de.json` (mirrored: `app`/`nav`/`footer`/`decks`/`study`/`stats`/`common`). `app/layout.tsx` is now async: `getLocale()` → `<html lang>` + `NextIntlClientProvider`. `components/shell/locale-switcher.tsx` (client) toggles EN↔DE via the `setUserLocale` Server Action (auto re-renders). Top-nav, footer, and all four route pages resolve every string through `useTranslations` / `getTranslations` (no hardcoded UI text). Verified via curl: no cookie → `lang="en"` + English; `wortbox.locale=de` cookie → `lang="de"` + German, server-rendered on first paint. `test` / `lint` / `build` all pass.

- **Feature 08 — stats & forecast:** Two new pure modules: `lib/content/forecast.ts` (`startOfDay` + `buildDueForecast(dueDates, now, days)` — calendar-day buckets keyed by day offset, overdue cards fold into today's bucket, cards beyond the window are dropped) and `lib/scheduler/projection.ts` (`projectIntervalCurve(initial, grade, params, now, steps)` — repeatedly calls the pure `scheduleNext` advancing `now` to each due date, capturing the growing interval; no persistence). New `lib/db` helper `getAllCards()`. UI under `components/stats`: `stats-view.tsx` (client; loads `getDeckSummaries` + `getAllCards` + `getSettings`, composes three token-styled panels), `deck-counts.tsx` (per-deck due/new/total table), `due-forecast.tsx` (14-day forecast over `scheduling.due`), `interval-curve.tsx` (card + grade Select driving the pure projection, intervals formatted via the existing `study.interval` i18n), and a shared hand-rolled `bar-chart.tsx` (flex/`bg-accent` bars, token colors only — no chart lib). `app/stats/page.tsx` now renders `StatsView`. New `stats` i18n keys (EN/DE): `loading`/`empty` + `decks`/`forecast`/`curve` groups. Pure tests for `buildDueForecast` (empty/overdue/by-day/out-of-window) and `projectIntervalCurve` (length, increasing due dates, easy≥good, unsupported-algorithm throw) — 8 new (69 total). `test` / `lint` / `build` all pass.

- **Feature 09 — import/export & A1 starter deck:** New `lib/io` boundary. `format.ts` defines the portable shape (`PortableCard = Omit<Card, "id"|"deckId"|"createdAt"|"updatedAt"|"scheduling">`, `PortableDeck`, `WortboxExport { format:"wortbox", version:1, decks }`, CSV column list, import-report types). `validation.ts` validates an `unknown` row into a `PortableCard` (typed guards for every field + the POS-presence invariant via the existing `getCardValidationErrors`), returning `{ok:true,card}` or `{ok:false,reasons}`. `json.ts` serializes/parses the multi-deck envelope (rejects wrong format / non-array decks). `csv.ts` has a hand-rolled RFC-style CSV tokenizer (quoted fields, `""` escapes, CRLF/LF) + a 12-column mapping (scalars plain, rich fields `tags`/`vowelMarks`/`noun`/`verb`/`adjective`/`related`/`examples` JSON-encoded per cell). `export.ts` (`cardToPortable`/`exportDeck`/`exportAllDecks`) and `import.ts` (`importParsed` → validates each row, creates a deck + `createCard` per valid card, returns `ImportResult` with per-deck `imported`/`rejected[]`). `download.ts` (client blob download + filename slug) and `starter.ts` (fetch `/starter-decks/index.json` manifest + a deck file). Starter content in `public/starter-decks/`: `familie.json` (8 cards) + `buero.json` (9 cards), fully modeled (gender, vowel marks on the bare headword, plural, verb forms, related words, opposites, 3 examples/card, verb examples cover all three forms), plus `index.json` manifest. UI under `components/io`: `import-dialog.tsx` (file → JSON/CSV parse → import → report with rejected-row reasons), `export-buttons.tsx` (per-deck JSON/CSV download), `starter-deck-dialog.tsx` (manifest list → one-click import). Wired into `deck-grid` (Starter + Import in the header toolbar) and `deck-detail` (Export JSON/CSV). New `io` i18n namespace (EN/DE) + `common.close`. Pure tests: validation (8), json round-trip (4), csv tokenizer + round-trip-through-validation (3), and a starter-content guard that re-validates every authored card against the model (7) — 22 new (91 total). `test` / `lint` / `build` all pass.

- **Feature 10 — AI card drafting (optional):** The app's only server code. `lib/io/ai.ts` is the shared contract + client helper: `AiDraftRequest {mode:"word"|"topic", input, topic}` / `AiDraftResponse {cards:PortableCard[], rejected}`, the pure `parseDraftRequest(unknown)` (input validation — mode whitelist, non-empty trimmed input ≤200 chars, non-empty topic), the pure `portableToDraft(PortableCard)→CardDraft` mapper (inverse of `buildCardFields`, joins adjective lists, defaults), and `requestAiDrafts()` (client `fetch("/api/ai")` → maps valid `PortableCard[]` to `CardDraft[]`). Server route `app/api/ai/route.ts` (POST): parses + validates the body via `parseDraftRequest`, reads `process.env.ANTHROPIC_API_KEY` server-side only (503 if unset, **no fallback**), calls the Anthropic Messages API via `fetch` (model `claude-sonnet-4-6`, forced `tool_choice` on an `emit_cards` tool whose `input_schema` mirrors the Card Data Model) with a system prompt encoding the A1 rules (bare headword/no article, vowel-mark indexing, gender hints, all three verb forms, 3–4 examples, POS-data-only-for-matching-POS), then **re-validates every model-produced card with the existing `validatePortableCard`** (model output treated as untrusted) and returns only valid cards + a rejected count. The key never reaches the client (network payload is `{cards,rejected}`). UI: `components/cards/ai-draft-dialog.tsx` (word/topic Tabs + input → `requestAiDrafts` → hands drafts back) wired into `card-list.tsx` next to **Add**; drafts feed a queue that opens the existing `CardEditorDialog` one at a time (new optional `initialDraft`/`subtitle` props; remount-by-key advances through the queue on save or skip). **Nothing persists until the user clicks Save in the editor** — each save is a normal `createCard`. New `cards.ai*` i18n keys (EN/DE). Pure tests for `parseDraftRequest` (valid/non-object/bad-mode/blank/too-long/missing-topic) and `portableToDraft` (noun mapping, passes `getCardDraftErrors`, round-trips through `buildCardFields`, adjective list join) — 10 new (101 total). **Env:** drafting requires `ANTHROPIC_API_KEY` set server-side; absent → the route returns 503 and the dialog surfaces the message. `test` / `lint` / `build` all pass.

## Feature Roadmap (`context/feature-specs/`)
1. `01-design-system.md` — shadcn/ui, `cn()`, design tokens (theme + gender + state), vowel-mark styling.
2. `02-app-shell.md` — top nav, content area, footer; responsive single-window layout.
3. `03-i18n.md` — `next-intl`, `en`/`de` locale files, language switcher, persisted language.
4. `04-card-data-model.md` — `lib/content` types + `lib/db` Dexie stores for cards/decks/reviewLog/settings.
5. `05-card-rendering.md` — front/back renderer: gender color, vowel marks, POS blocks, examples.
6. `06-deck-and-card-management.md` — deck CRUD, structured card editor with live preview, tags/topics.
7. `07-review-session.md` — `lib/scheduler` (FSRS via `ts-fsrs`), due queue, flip/grade, interval previews.
8. `08-stats-and-forecast.md` — per-deck counts, due forecast, per-card interval curve.
9. `09-import-export-and-a1-starter-deck.md` — CSV/JSON I/O + curated A1 starter deck.
10. `10-ai-card-drafting.md` — optional `app/api/ai` proxy producing model-shaped draft cards.
11. `11-a1-starter-content-expansion.md` — author full A1 topic coverage (content only; reuses Feature 09 mechanism). Highest-priority gap from `gap-analysis.md`.
12. `12-connected-vocabulary-web.md` — synonyms/opposites on all POS + navigable related-word links + cluster study. Gaps #1 + #2 from `gap-analysis.md`.
13. `13-frequency-and-level.md` — frequency rank + CEFR level fields, frequency-ordered new-card introduction, level filtering. Gap #3 (prerequisite for A2/B1, gap #4).

## In Progress
- None.

## Next Up
- Roadmap (01–10) complete. Remaining work is polish/expansion: broaden the A1 starter content (see Open Questions) and tune scheduler/AI parameters.

## Open Questions
- Starter-deck content scope: an initial set ships (Familie 8 + Büro 9 = 17 fully-modeled cards). Expanding to the learner's full A1 source (e.g. Netzwerk A1 chapters) still needs the owner's chapter/word-list confirmation — the importer/schema/manifest are in place, so expansion is additive (drop new files in `public/starter-decks/` + add a manifest entry; `starter-content.test.ts` guards validity).

## Architecture Decisions
- v1 is local-first: IndexedDB (Dexie) is the single source of truth; no server DB, no auth. (`architecture-context.md`)
- Cards carry both languages; presentation hints are derived from card data, never stored as styling.
- Study direction (English-front default / German-front / Mix) is a presentation setting over a **single shared schedule** — it flips the prompt side but never changes a card's due date or creates a second review item. Stored in `settings`.
- Gender color mapping: masculine → blue, feminine → red, neuter → green.
- Vowel length: long → underline, short → under-dot (Netzwerk convention).
- UI is bilingual (EN/DE) via `next-intl`; card content is data, not i18n keys.
- Default scheduler is **FSRS via `ts-fsrs`** (chosen after comparing npm options — the clear standard, ~64k weekly downloads, TS-native, browser-safe) behind one stable `lib/scheduler` interface; SM-2/Leitner remain swappable. We own the interface and parameters, not the algorithm math. `ts-fsrs` is added as a dependency in Feature 07.
- The only server code is the optional AI proxy route (`app/api/ai`).

## Session Notes
- 2026-06-17: Reviewed and completed the context folder — removed residual references to the source starter template, fixed the broken color table, realigned `code-standards.md` to the local-first architecture.
- 2026-06-17: Named the project **Wortbox** across package, app metadata, page, and context docs.
- 2026-06-17: Captured the full German A1 problem statement in the context docs and produced the feature-spec roadmap (01–10).
- 2026-06-17: Added switchable study direction (English-front default / German-front / Mix) as a presentation setting over a shared schedule; threaded through overview, architecture, UI, and specs 04/05/07.
- 2026-06-17: Compared spaced-repetition npm packages and chose **FSRS via `ts-fsrs`** as the v1 algorithm (instead of hand-rolling SM-2); updated overview, architecture, and specs 04/07.
- 2026-06-18: Completed Feature 01 (design system & UI primitives) — verified tokens, light-mode values, `@theme inline` mappings, vowel-mark CSS, `cn()`, all 10 shadcn components, lucide-react, and Geist fonts. `test`, `lint`, `build` all pass. Resolved the light-mode open question.
- 2026-06-18: Completed Feature 02 (app shell) — top nav, footer, layout wiring, and placeholder route pages. **Decision:** the spec lists a "Study" nav item but defines only `/study/[deckId]`; added a `/study` placeholder landing as the natural parent so the nav item has a destination (pick a deck). Nav active state matches `/study` and `/study/[deckId]`. Updated the home page test to assert the new "Decks" heading.
- 2026-06-19: Completed Feature 04 (card data model + Dexie storage). **Decision (owner-confirmed):** UI-language migration uses **settings-canonical + cookie-mirror** — the IndexedDB `settings` store is the source of truth for `uiLanguage`, but the `wortbox.locale` cookie is kept in sync as the SSR transport (IndexedDB is client-only and can't be read during SSR), preserving the Feature 03 first-paint-correctness decision. The switcher writes both; `LocaleSync` reconciles on load (settings wins). Other notes: `due` stored as epoch-ms `number` (not `Date`) for uniform IndexedDB indexing — the Feature 07 scheduler wrapper will convert to/from `ts-fsrs`'s `Date`. `SchedulingState` is a discriminated union keyed on `algorithm`; SM-2/Leitner variants are defined now so storage never changes when an algorithm is swapped. Helper tests cover the pure derivation functions only (db helpers are I/O, untested here per spec). `dexie@4` installed with `--store-dir ./.pnpm-store/v3` (project uses a local pnpm store).
- 2026-06-19: Completed Feature 05 (card rendering). **Decision:** reconciled "prompt is minimal chrome" with "the full German block always appears on the German side" — `card-reveal` always renders the shared `GermanDetail`; in German-front the reveal adds the English headword above it (the prompt stays headword-only), so the same `GermanDetail` component is reused on whichever side German occupies. German tense labels (Präsens/Präteritum/Perfekt) are routed through i18n but kept as the German terms in both locales (pedagogically standard for A1). Gender-ending/group hints prefer a stored `NounData` value when present, else derive via the Feature 04 helpers. Adjective comparative/superlative fields exist on the model but are out of this spec's scope (spec lists only opposites + synonyms as chips) — left unrendered.
- 2026-06-19: Completed Feature 06 (deck & card management). **Decision:** the deck list (`/`) links each deck to a new `/decks/[deckId]` management page (card list + structured editor) — distinct from `/study/[deckId]` (review session); the spec implies a card-management surface ("filter a deck's cards") without naming a route, so management lives under `/decks/*` and study under `/study/*`. **Decision:** the editor's draft validation (`getCardDraftErrors`) is separate from `lib/content/validation.ts` — the latter enforces the POS-data-presence invariant on saved `Card`s, the former validates the looser editable `CardDraft` (empty strings, unset gender) and is the save gate; `buildCardFields` is lenient (omits incomplete POS data so the live preview can render mid-entry) while the validation gate blocks the actual save. **Lint note:** React 19's `react-hooks/set-state-in-effect` rule forbids synchronous `setState` in effects — form/editor reset-on-open is handled by remounting via a bumped `key` prop (no effect), and data-load effects use a lexical `await`-then-`setState` IIFE with an `active` cancellation flag. `test` / `lint` / `build` all pass.
- 2026-06-19: Completed Feature 07 (review session & scheduler). **Decision:** `scheduleNext` is implemented *on top of* `previewGrades` (picks the graded outcome from the same `repeat()` result) rather than calling `next()` separately — this guarantees the interval shown on a grade button is exactly what gets persisted, satisfying the spec's preview==apply check by construction. **Decision (model change):** added `learningSteps: number` to `FsrsSchedulingState` because ts-fsrs's `Card.learning_steps` drives (re)learning-step intervals; without persisting it every reload would reset the step index and produce slightly wrong short-term intervals. Updated `initialSchedulingState` and the two hand-built fsrs test fixtures. **Decision (entry point):** a session must be reachable, so deck-detail got a primary **Study** button → `/study/[deckId]`; the `/study` landing stays a placeholder for now. **Lint note (React 19 `react-hooks/purity`):** `Math.random()`/`Date.now()` cannot be called during render (incl. `useMemo`). Resolved without disabling the rule — both impure values are produced only in event handlers and the async loader and stored in state (`now`, `mixCoin`); render-phase memos read those pure values. A welcome side effect: the stored `now` is shared by preview and apply within a card turn, so they use an identical timestamp. Mix randomization re-rolls `mixCoin` on advance and on switching to Mix.
- 2026-06-19: Completed Feature 08 (stats & forecast). **Decision:** forecast and interval curve are kept as **pure** functions (`lib/content/forecast.ts`, `lib/scheduler/projection.ts`) — no new system boundary; the client `StatsView` does all I/O via existing `lib/db` helpers (+ new `getAllCards`). **Decision:** charts are hand-rolled flex/`div` bars (`components/stats/bar-chart.tsx`) using only color tokens (`bg-accent`/`bg-brand`) rather than adding a chart dependency, per the spec's "keep dependencies light." **Decision:** forecast window is **14 calendar days** ("days and weeks"); overdue cards fold into today's bucket and cards due beyond the window are dropped from the chart. **Decision:** the interval curve projects from the selected card's **current** scheduling state (not a fresh card) over 8 successive same-grade reviews (default Good; Hard/Good/Easy selectable), simulating reviewing exactly on each due date; intervals reuse the Feature 07 `describeInterval` + `study.interval` i18n for human labels.
- 2026-06-20: Completed Feature 09 (import/export & A1 starter deck). **Decision (export format):** the portable format carries card **content only** — it omits `id`, `deckId`, `scheduling`, and timestamps. On import every card is created fresh via `createCard` (new id, fresh scheduling, current timestamps), so a user export and the static starter deck share **one** format and imported cards are byte-for-byte indistinguishable from user-created ones (satisfies the spec invariant). "Round-trip without data loss" therefore means **content** fidelity, not study-progress fidelity — scheduling is study state, not portable content. **Decision (multi-deck envelope):** JSON is always `{format,version,decks:[{name,description,cards}]}` — single-deck export emits one entry, "export all" emits many, and each starter file is a one-entry envelope, so there is exactly one JSON parser. CSV is single-deck (no deck wrapper); the import deck name defaults to the file name. **Decision (CSV mapping):** 12 columns — scalar fields (`partOfSpeech`/`english`/`german`/`topic`/`notes`) plain, rich fields (`tags`/`vowelMarks`/`noun`/`verb`/`adjective`/`related`/`examples`) JSON-encoded per cell; a hand-rolled tokenizer handles quoting (kept dependencies light, no CSV lib). **Decision (starter manifest):** added `public/starter-decks/index.json` (a manifest of `{id,file,name,description,cardCount}`) so the UI can list decks without hardcoding filenames; not previously in the architecture doc but a natural extension of the documented `public/starter-decks` location. **Note (vowel marks):** `card.german` is the bare headword (the article is rendered from gender), so authored `vowelMarks` index into the bare word; loanword/diphthong vowels (Computer, klein, neu, schreiben) are left unmarked. **Lint note:** the starter dialog's load effect only `setState`s after an `await` (inside an `active`-guarded IIFE), gated on `open`, to respect React 19's `react-hooks/set-state-in-effect`.
- 2026-06-20: Completed Feature 10 (AI card drafting). **Decision (no SDK):** the route calls the Anthropic Messages API directly via `fetch` rather than adding `@anthropic-ai/sdk` — keeps the single server route thin and adds no dependency (per owner request to skip the SDK). **Decision (trust boundary):** the model returns `PortableCard`-shaped objects through a forced `emit_cards` tool, and the route re-validates each with the existing `lib/io` `validatePortableCard` before returning — so the AI path reuses the exact validation the import path uses and untrusted model output can never become an editable card unchecked. Invalid cards are dropped and counted (`rejected`), not surfaced as broken drafts. **Decision (editor reuse):** drafts are not a new surface — they flow into the existing Feature 06 `CardEditorDialog` (gained optional `initialDraft`/`subtitle`), one draft at a time via a queue in `card-list` that advances on save *or* skip by bumping the remount key; topic mode yields 6–8 cards, word mode 1. Nothing is written to IndexedDB until the user clicks Save (normal `createCard`), satisfying the never-auto-saved invariant. **Decision (model):** `claude-sonnet-4-6` — capable enough for structured German A1 grammar, faster/cheaper than Opus for this shaping task. **Env:** introduced `ANTHROPIC_API_KEY` (server-only, no fallback per project rules); unset → route returns 503 and the dialog shows the configured-failure message. The roadmap (01–10) is now complete.
- 2026-06-18: Completed Feature 03 (i18n) with `next-intl@4` in no-routing mode. **Decision (owner-confirmed):** persist the interim UI-language choice in a **cookie** (`wortbox.locale`) instead of the spec's `localStorage`, because the cookie is readable server-side so SSR renders the correct locale on first paint — `localStorage` would cause a client-only resolution with a hydration mismatch/flash. Updated `03-i18n.md` to record this. Resolved the i18n-library open question (kept `next-intl` per "continue with feature 3"). Routes are now dynamic (`ƒ`) since they read the cookie. Pages use `useTranslations` (sync, server-component-safe); the async `/study/[deckId]` page uses `getTranslations`. Migrate the cookie to the `settings` store in Feature 04.
